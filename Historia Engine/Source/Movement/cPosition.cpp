#include <Movement\cPosition.hpp>



namespace zkt {


	/*cPosition::cPosition(float x, float y)
	{
		m_position.x = x;
		m_position.y = y;
	}

	const sf::Vector2f& cPosition::getPosition() const
	{
		return m_position;
	}

	void cPosition::setPosition(float x, float y)
	{
		m_previousPosition = m_position;
		m_position.x = x;
		m_position.y = y;
	}

	void cPosition::setToPreviousPosition()
	{
		m_position = m_previousPosition;
	}*/
}